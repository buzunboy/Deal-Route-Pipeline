dist/
node_modules/
coverage/
*.md

# Generated by drizzle-kit — keep its own formatting, don't let prettier churn it.
drizzle/

# Golden test fixtures are byte-stable snapshots of real pages — never reflow them
# (reformatting would alter the text the extraction grounding check matches against).
test/fixtures/

# API contract artifacts: the Postman collection is generated by `npm run api:postman`
# (let the tool own its formatting, like drizzle/), and the OpenAPI spec is kept beside
# it — prettier would churn the generated JSON on every regen.
docs/api/
