# DealRoute — Data Pipeline Service

The crawl / LLM-extraction / verification / monitoring service for **DealRoute** (a verified search engine for subscription bundles, Germany v1). It turns web sources into evidence-backed **deal records** that a human approves before publish. Standalone repo — the landing page and the production admin panel live in separate repos.

## Docs map (what each doc is, its status, and the source of truth)

Read this before reaching for any `docs/` file. **Statuses:** _LIVING_ = update as work changes · _EVERGREEN_ = founding reference, rarely changes · _HISTORICAL_ = a delivered build plan, do NOT treat as pending work. Detailed coding standards auto-load from `.claude/rules/` (`architecture.md`, `code-style.md`, `extraction-and-schema.md`, `testing.md`, `api-and-openapi.md`) — those are binding.

**START HERE / source-of-truth (LIVING — keep current):**
- `docs/DealRoute_Status_and_Roadmap.md` — **the ONE living status + roadmap doc.** What's built, what's left, the env/workflow gotchas — merges the former PostP3 handoff + the Phase-C roadmap. Currently: **all post-C Steps 1–6 DONE + merged, the admin `/api/*` surface extended + contract-aligned, and the API LIVE on Fly** — no roadmap step remains; everything outstanding lives in `docs/KNOWN_ISSUES.md`. _Update §2 (built) + §3 (left) whenever work ships._
- `docs/KNOWN_ISSUES.md` — **the deferred-findings register** (open + resolved) — the actual to-do list now. _Append on every deferred finding; move to Resolved when fixed (see Working habits)._
- `docs/testing/LIVE_TEST_TEMPLATE.md` — **the live-test results template** (+ filled runs in `docs/testing/results/`). _Use for every real/live test; EXTEND it when a feature adds a recordable field (see Working habits → Live testing)._
- `docs/handoffs/` — **cross-repo handoffs to the admin-panel project** (separate repo): `ADMIN_PANEL_metrics_endpoints.md` (ACR-6/9/10 metrics + settings wiring) + `ADMIN_PANEL_evidence_fetch.md` (evidence now loads from the gated `GET /api/evidence/:id/:artifact` — panel must fetch-with-Bearer → blob URL). _Add a handoff doc here whenever a pipeline change requires a coordinated panel-side change._

**Reference (EVERGREEN — read on demand):**
- `docs/DealRoute_Crawl_Pipeline_Plan.md` — the founding design (principles, 4 tiers, 2 lanes, deal-record shape, trust invariants). The "why it's built this way."
- `docs/DealRoute_Seed_List_DE.md` — the DE seed sources (provider/bundler/community URLs).
- `docs/Firecrawl_Integration_Reference.md` — Firecrawl (search+scrape vendor behind ports; v2; Tier-4 backend). Key lives in gitignored `.env` only.

**Historical build records (DELIVERED / SUPERSEDED — banner-marked; NOT pending work):**
- `docs/DealRoute_PostP3_Handoff.md` + `docs/DealRoute_Phase_C_and_Roadmap.md` — **SUPERSEDED (2026-06-22)** by `docs/DealRoute_Status_and_Roadmap.md`; kept for their per-step "as-shipped" rationale + the Phase-C / agentic-discovery design.
- `docs/DealRoute_P3_PublicAPI_Handoff.md` (P3 = Step 1), `docs/DealRoute_Phase_C_Plan.md` (C-1/C-2), `docs/DealRoute_Hardening_Plan.md` (post-C-1 hardening), `docs/DealRoute_PostC_Handoff.md` (post-C audit). Kept for design rationale; their "next steps" are stale by design.

**Doc-hygiene rule (do this, don't let docs rot):** when you ship work, update the LIVING docs above in the same change; banner a build plan ✅ DELIVERED once its work merges; never leave two docs claiming to be "the current next step." Keep this map accurate when you add/retire a doc.

## Non-negotiable invariants (always)
- **LLM proposes, humans approve.** Extraction produces *candidates* with a confidence score + grounding snippets. **Nothing auto-publishes** in v1.
- **Evidence required.** Every candidate stores screenshot + HTML + terms text + source URL + timestamp before review.
- **Best-effort read any page (2026-06-21; supersedes "public pages only").** The pipeline reads ANY page it can fetch a body from — robots-disallowed and login-walled pages included. `RESPECT_ROBOTS_TXT` **defaults `false`** (the robots gate stays, opt-in via `=true`); a login-wall / soft-block page is read best-effort (the fetcher returns `ok` + a `fetchSignal`, the candidate stays must-review). Still true: the **per-domain rate-limit always applies** (robots-off ≠ hammering hosts); a **`captcha` challenge** (no offer content) still routes to manual-capture; **soft-404 / maintenance / expired interstitials** are still skipped (`error`) so a stale/wrong deal can't be manufactured; the fetcher **never logs in** — no credential system exists yet (real auth is deferred "later" work). _Legal note: ignoring robots/ToS changes legal exposure (EU/DE) — a business decision the owner made deliberately._
- **Extensible schema, never invented columns.** Typed core + `conditions[]` / `attributes` / `raw_conditions_text`; unknown conditions become `field_proposals` (see `.claude/rules/extraction-and-schema.md`).
- **Never trust raw LLM or scraped data.** Validate at the boundary into typed domain objects.

## Architecture (see `.claude/rules/architecture.md`)
Clean, layered: **domain** → **application** (use-cases) → **adapters/infrastructure**. Dependencies point inward; the domain imports no vendor SDK. Program to **ports**; inject concrete adapters from **one composition root**; every Fetcher / BrowserAgent / LLM / EvidenceStore / DB / Queue is swappable via config. Models are configurable via env (cheap extractor + stronger discovery model) — no hard-coded vendor clients in business logic.

## Commands
Stack: **TypeScript (Node 20+, strict)** · zod · Playwright/Firecrawl · Anthropic/OpenAI/stub · Postgres+drizzle · Vitest.
- Install: `npm install && npx playwright install chromium`
- Build: `npm run build`
- Test: `npm test` (fast hermetic unit/contract/golden/HTTP). Integration (real Postgres): `npm run test:integration` (needs `DATABASE_URL_TEST`). Live smoke (real sites+LLM, scheduled): `npm run test:live` (needs `RUN_LIVE_TESTS=1` + key)
- Lint + typecheck: `npm run lint && npm run typecheck` (or `npm run check` for both + tests)
- Dry-run extract a URL: `npm run cli -- dry-run-extract <url|file>` (no writes; `LLM_PROVIDER=stub` for offline)
- Run a crawl: `npm run cli -- crawl --source <id> | --subscription <name> | --due [--dry-run]`
- Discover a site (Lane B): `npm run cli -- discover <url> [--max-pages N] [--dry-run]` (bounded same-site crawl → candidates + proposed novel domains)
- Broad discovery (Tier 4 / Phase C C-1): `npm run cli -- discover --broad [query] [--max-steps N] [--max-queries N] [--dry-run]` (agentic: search → fetch public results → extract → propose novel domains; catalog-driven or one explicit query). Needs `AGENT=search` + a search backend (`SEARCH_API_KEY` for Brave, or `SEARCH_PROVIDER=firecrawl`); default `AGENT=noop` runs nothing. Capped by steps/queries/€/time + the daily budget; nothing auto-publishes, no discovered domain is auto-crawled.
- Ingest a community feed (Lane B / Tier 3): `npm run cli -- ingest --source <id> | --community-due [--max-items N] [--dry-run]` (RSS → triage → extract relevant leads)
- Monitor / Review / Serve: `npm run cli -- monitor --due` · `review list|edit|approve|reject|proposals|promote|manual|complete-manual|sources|approve-source|reject-source` · `serve` (one port: the gated admin API `/api/*` + test page, AND the PUBLIC read-only `/v1/*` feed over published deals — see Public read API below). New review actions (CLI + `/api/*`): `review edit <id> <who> '<patch-json>'` corrects a candidate's reviewer-editable fields before approve; `review promote <key> <who> <canonical_key> '<label>'` promotes a recurring field proposal into the vocabulary; `review complete-manual <task> <who> '<fields-json>' '<evidence-json>'` turns a manual-capture task into an evidence-backed candidate.
- Gated admin write/read API (`/api/*`, Bearer-gated writes): `GET /api/candidates?status=&service=&confidence_max=&limit=&offset=` (review queue, filtered + paginated, lowest-confidence first; defaults to candidate+in_review; each item's `evidence` is the admin DTO — raw `*_ref` store keys PLUS `evidence_screenshot_url`/`evidence_html_url`/`evidence_terms_url` as RELATIVE authed paths to `GET /api/evidence/:id/:kind` (NOT the public CDN; always present), so the panel renders the screenshot + links the HTML/terms, ACR-13) · `PATCH /api/candidates/:id {approver, patch}` (reviewer edit — allowlisted fields only via the pure `applyCandidatePatch`; re-validates; tags every changed field in the deal's `human_edited` so a corrected value is never read as model-grounded; KEEPS the model grounding quotes; status stays `candidate`; audited `action:'edit'`; a later approve publishes the edited record) · `POST /api/candidates/:id/approve` · `/reject` · `GET /api/candidates/:id/reviews` · `GET /api/evidence/:id/:artifact` (Bearer-GATED reviewer evidence-fetch — streams ONE artifact's bytes (`artifact` ∈ screenshot|html|terms) from the `EvidenceStore` with `Cache-Control: private, no-store`; the authed complement of the screenshot-only public CDN, so reviewers reach the html/terms the CDN 403s; `evidence.json`/metadata NOT served; unknown id/kind → 404; the only gated GET) · `GET /api/field-proposals` · `POST /api/field-proposals/:key/promote {approver, canonical_key, label, target}` (adds a `condition_vocabulary` row with the suggested key as an alias + marks the proposal `promoted`; `target:"field"` → 400 not_supported, deferred) · `GET /api/manual-capture-tasks` · `POST /api/manual-capture-tasks/:id/complete {approver, fields, evidence}` (evidence REQUIRED — `{source_url, screenshot_ref, html_ref, terms_ref, terms_text}`, screenshot by **reference** not bytes; mints a `candidate` from the human fields, marks the whole record `human_edited`, never auto-publishes) · `GET /api/sources/pending` (each Source carries `proposal_reason` — the discovery/ingest "why proposed" rationale, ACR-15) · `POST /api/sources/:id/approve|reject` · `GET /api/sources/:id/reviews`. **Admin-panel endpoints (ACR-5→12, the new-endpoint set):** `GET /api/candidates/counts` (review-queue aggregate counts — all_pending/low_confidence/human_edited/by_route + date-bounded rejected_today, ACR-5) · `GET /api/audit?actor=&entity_id=&since=&limit=` (cross-deal recent-activity/audit feed over the reviews log — approve/reject/edit only; promote/extract deferred, ACR-7) · `GET /api/published?limit=&offset=` (admin publication-history: published+expired, status-mapped live|unpublished, ACR-10) · `GET /api/sources` + `POST /api/sources {approver, domain, kind, tier}` (operational sources registry + "+ Add source"; kind capitalised, status active|degraded|disabled, ACR-10) · `POST /api/manual-capture-tasks {approver, fields, evidence}` (ad-hoc capture, NO backing task — mints a done `ad_hoc` task + candidate, ACR-12) · `GET /api/team` + `POST /api/team {approver, name, email, role?}` + `PATCH /api/profile {approver, name}` (reviewer registry — pipeline is system-of-record; review_count derived from the reviews log; ACR-10/11) · `GET /api/alerts` (+ open_count for the bell) + `POST /api/alerts/:id/acknowledge|resolve` (persisted alerts with read-time auto-resolve; ACR-8). **Metrics/aggregation layer (ACR-6/9/10-Metrics, on the extended `MetricsUseCase`):** `GET /api/metrics/throughput?period=today` → `{approved, rejected, edited, avg_review_seconds}` (today's UTC reviewer decision counts + the mean capture→decision latency in seconds, null when none; `avg_review_seconds` is the ACR-doc NUMBER shape — the panel zod migration to it is handed off in `docs/handoffs/ADMIN_PANEL_metrics_endpoints.md`; ACR-6) · `GET /api/candidates/freshness` → `[{bucket: "<24h"|"1-3d"|">3d", percent}]` (pending-queue age distribution, aged by `now − evidence.captured_at`, three bands always summing to 100; ACR-9) · `GET /api/metrics` → `{kpis, cost_per_day, confidence_distribution}` (KPI cards + a dense 14-UTC-day cost series + pending-confidence bands High≥0.8/Med0.6–0.79/Low<0.6; ACR-10 Metrics). All three are PURE projections (`src/domain/metrics/{throughput,queue-freshness,dashboard-metrics}.ts`) over `crawl_runs` + `reviews` + `deals` via new ports `DealRepository.pendingQueueSignals` + `ReviewRepository.listDecisionLatenciesSince` — both adapters + contract suite, NO schema/migration. **Settings (ACR-10 Settings, `SettingsUseCase`):** `GET /api/settings` → `{groups: [{key, label, rows: [{key, label, control:"toggle"|"value", enabled|value, read_only}]}]}` (env-driven config stays the source of truth; a `settings` table (migration `0018`) stores ONLY overrides for the writable knobs) + `PATCH /api/settings/:key {approver, value}` → `{key, updated}` (writable knobs ONLY — `affiliate_disclosure` (the approve default, immediate) + `daily_budget_queued` (stamped with `DEPLOYMENT_ID`; the NEXT deployment's `Container.init()` → `SettingsUseCase.consumeQueuedBudget()` ADOPTS a prior-deploy queued value as that process's `DailyBudgetGuard` ceiling via `setCeiling` + DELETES the row (self-clear); the in-effect `daily_budget` is a read-only mirror); a read-only/unknown key → 409 `SettingNotWritableError`, an invalid value → 400). `Container.init()` is awaited by `serve` + the budget-using lanes (crawl/monitor/discover/ingest). New config `DEPLOYMENT_ID` (default `local-dev`; prod sets per-release for the queued-budget self-clear). Panel-side migration handoff: `docs/handoffs/ADMIN_PANEL_metrics_endpoints.md` §4. Deal-record **schema v5** (migration `0013`): added `human_edited: string[]` (additive, default `[]`; never LLM-proposed). **Source schema** gained `proposal_reason: string|null` (migration `0015`, additive/nullable; ACR-15). **Admin-panel tables:** `team_members` (migration `0016`, ACR-10/11) + `alert_events` (migration `0017`, partial-unique one-open-row-per-dedupe_key, ACR-8) + `settings` (migration `0018`, key-PK override store, ACR-10 Settings). Latest migration `0018`.
- Public read API (P3): `GET /v1/deals?service=&country=&route_type=&price_max=&sort=cost_asc|verified_desc&limit=&offset=` → `{ deals, total, limit, offset }`; `GET /v1/deals/:id` (404 unless published); `GET /v1/health`. Unauthenticated, read-only, `published` only; curated DTO leaks NO internal field (contract-tested) + a coarse freshness `trust` badge + the EU-Omnibus `affiliate_disclosure` (bool, default true) + `published_at` (Step 2, set by the reviewer at approve; CLI `--no-affiliate-disclosure` / `/api` approve body `affiliate_disclosure`) + `human_edited: string[]` (v5 — field paths a human corrected/entered, so a consumer never shows a corrected value as model-grounded; field-path strings only, no raw data); `evidence_screenshot_url` resolved from `S3_CDN_BASE_URL` (null when unset). CORS via `PUBLIC_CORS_ORIGIN` (default `*`); page size hard-capped (`PUBLISHED_MAX_LIMIT`). `/v1/*` dispatch is total — never falls through to `/api/`. **Sort is reliability-blended (Step 3):** the primary key (`cost_asc` true-cost / `verified_desc` freshness) decides order; ties break by the deal's source `reliability_score` DESC (read-time registrable-domain deal→source join; neutral 0.5 when no source matches) then `id`. The raw reliability score is NEVER exposed — order-only; the freshness `trust` badge stays the sole public trust signal. No schema/migration change; one pure ranker (`published-ranking`) shared by both DB adapters (LSP); `countPublished` is order-invariant.
- Cost stats: `npm run cli -- stats [--since YYYY-MM-DD] [--until YYYY-MM-DD] [--runs]` (aggregate logged `crawl_runs.cost_eur` — total + per UTC day + per source; half-open window: since inclusive, until exclusive; `--runs` also lists recent runs: kind/candidates/proposals/cost/stop-reason). All lanes (crawl + discover + discover_broad + ingest) log a `crawl_runs` row, so cost/metrics cover the agentic lane. A daily €-budget guard caps total agentic spend per UTC day via `DAILY_BUDGET_EUR` (default 10.00; 0 disables).
- Seed import: `npm run cli -- seed-import` · DB: `npm run db:migrate`
- API contract: `npm run api:lint` (redocly-validate `docs/api/openapi.yaml`) · `npm run api:postman` (regenerate the Postman collection from the spec — finalize strips the generator's random ids so the output is byte-stable) · `npm run api:check` (CI gate = `api:lint` + `api:postman:check`, a STRUCTURAL drift check that regenerates the collection and compares folders/requests/methods/paths to the committed one — fails if an endpoint was added/renamed/removed in the spec but not regenerated; runs in CI). Update the spec whenever the HTTP API changes — see `.claude/rules/api-and-openapi.md`.

## Repo layout
- `src/domain/` — deal-record schema (zod) [+ `human_edited: string[]` (schema v5): field paths a human set/corrected post-extraction (reviewer PATCH + manual capture) — the trust trail so a corrected value is never read as model-grounded; never LLM-proposed] [+ `published-query` (public-feed filters/sort/pagination types + `PUBLISHED_MAX_LIMIT` page cap) + `published-ranking` (Step 3: the ONE pure public-feed ranker — primary key → source-reliability tiebreaker (registrable-domain deal→source join, neutral 0.5) → id; `PUBLISHED_FETCH_CAP` bounded fetch; shared by both DB adapters for LSP-identical order; raw reliability never leaves the comparator)] [+ `candidate-query` (the gated review-queue filter/pagination types — `CandidateFilters` status/service/confidenceMax + caps + `REVIEWABLE_STATUSES`; sibling to published-query)] + pure rules: `rules/{true-cost,dedupe-key,vocab-mapping,validate-record,confidence,candidate-patch [the reviewer-edit allowlist `PATCHABLE_FIELDS` + `applyCandidatePatch` — enforces the editable set, boundary-validates each sub-object, merges immutably, re-derives true_cost on a price change, reports changed paths; identity/provenance/status are never editable]}`, entities (source [+ `resolved_url` (Step 4): the post-redirect finalUrl, set on first successful crawl/monitor pass; monitor matches expiry/baseline on `resolved_url ?? url`] [+ `registrable_domain` (Step 6): the eTLD+1 of `url`, pinned via the real PSL at source-create/seed-import; the reliability join keys off it], evidence [+ `assertCaptureComplete` hollow-bundle guard, `missingReferencedEvidence` (the manual-capture twin — checks the by-reference bundle: screenshot/html/terms refs + inline terms text + url all non-empty), `evidence-layout` (the one place a bundle's file names + per-artifact content-types + the `EvidenceArtifactKind` union/`EVIDENCE_ARTIFACTS` map + the `<id>/screenshot.png` ref are defined — shared by both stores, the public DTO, and the gated evidence-fetch endpoint)], crawl [CrawlRun: nullable source_id + `run_kind` crawl|discover|ingest|discover_broad + `stopped_reason` …|step_cap + proposals_produced], proposals, monitoring, review/{review-record,source-review-record}, catalog/subscription, metrics/{cost-summary [CostSummary schema + `roundEur` half-up-to-cents + `SOURCELESS_RUN_BUCKET` sentinel], daily-budget [pure €/UTC-day guard rules]}), `discovery/{links,community-keywords,triage-result,untrusted-text [prompt-injection framing],broad-queries [Tier-4 query building],domain-denylist,suffix-oracle [Step 6: the pure `SuffixOracle` TYPE — `(url)=>registrable-domain|null`; zero imports; the real PSL lives behind it in an adapter so dedupe/ranking stay vendor-free + sync]}` (pure link/keyword rules + frontier scoring + triage boundary), `markets/markets` (Step 6: the `MARKETS` registry — in-scope countries → allowed currencies; the closed `Country`/`Currency` enums + the per-country currency trust rule derive from it; "add a country" = one data row), `alerting/alert-event` (Step 5: vendor-neutral `AlertEvent` + pure builders for the source-reliability-low + daily-budget-reached signals; dedupe keys; no I/O), `metrics/{throughput,queue-freshness,dashboard-metrics}` (ACR-6/9/10-Metrics: pure dashboard projections — today's reviewer counts + capture→decision latency; pending-queue age buckets; KPI/cost-series/confidence rollups; largest-remainder %→100), `settings/{setting-override,settings-catalog}` (ACR-10 Settings: the override entity + the settings CATALOG — every knob's group/label/control/kind (writable|read_only) declared once + the pure `buildSettingsView` override-over-live merge + `validateSettingValue` per-key rule), typed errors, `parse-llm-output` (LLM boundary)
- `src/application/` — use-cases (`extract`, `crawl/crawl-source`, `crawl/candidate-sink` (shared persist), `crawl/run-recorder` (shared crawl_runs start/finish/fail for the bounded lanes), `review` (now also: `editCandidate` reviewer field-correction with `human_edited` tagging; `promoteFieldProposal` proposal→vocabulary; `completeManualCapture` manual-capture task→evidence-backed candidate; `listCandidates(filter)` queue filtering/pagination — all approver-gated + audited, none auto-publish), `review/source-review` (source-promotion loop), `monitor/monitor-source` (re-verify; reliability/back-off via the shared `crawl/source-policy.applyCrawlOutcome` — same policy as Lane A), `metrics/metrics` (cost aggregation + recent-runs over `crawl_runs`; ALSO the ACR-6/9/10-Metrics dashboard rollups — `throughputToday`/`queueFreshness`/`dashboardMetrics`, takes a Clock for the UTC "today"), `metrics/daily-budget-guard` (aggregate €/UTC-day ceiling across a batch), `settings/settings` (ACR-10 Settings: `getSettings` env-mirror+override merge, `updateSetting` writable-only PATCH with the queued-budget `DEPLOYMENT_ID` next-deploy stamp, `defaultAffiliateDisclosure` for the approve default), `discover/discover-site` (Lane B; writes a run, mid-loop cost guard, bounded frontier), `discover/discover-broad` (Tier-4 broad discovery C-1; query set → BrowserAgent → extract → propose → run, shared budget + deny-list), `discover/lane-b-support` (shared Lane-B edge logic), `ingest/ingest-community` (Lane B Tier 3; writes a run) + `ingest/triage-prompt`, `discover/noop-browser-agent`) + `ports/` (Fetcher, FeedReader, Llm, EvidenceStore [+ `getArtifact(id,kind)` → bytes+content-type for the gated reviewer evidence-fetch; read-only, never mutates a write-once bundle], SearchProvider, repositories+Database [`CrawlRunRepository` costSummary/spentSince/recentRuns; `DealRepository` listPublished/countPublished for the public feed + `listCandidates(filter)` for the gated review queue; `FieldProposalRepository` getByKey/markPromoted; `ManualCaptureRepository` getById/markDone; the new `ConditionVocabularyRepository` getByKey/upsert/list (the table predates it; this is its first typed port — promotion writes here); `DealRepository.pendingQueueSignals` + `ReviewRepository.listDecisionLatenciesSince` (ACR-6/9/10-Metrics dashboard reads); `SettingsRepository` get/list/upsert/delete (ACR-10 Settings override store)], Queue, Clock, BrowserAgent [thin; returns fetched pages, not extractions], Logger, Alerting [Step 5: best-effort `alert(event)` — never throws, so it can't crash a lane])
- `src/adapters/` — `fetcher/` (playwright, browser-render [C-2: local Playwright JS-render for JS-heavy SPAs — networkidle + scroll, shares `playwright-capture`], firecrawl, page-classifier [best-effort-read: login-wall/soft-block → `ok`+`fetchSignal`; captcha + soft-404 stay non-`ok`], polite-fetcher [per-domain rate-limit ALWAYS + robots OPT-IN (`RESPECT_ROBOTS_TXT`, default off); injectable `RobotsClient` seam]) · `search/` (stub [default off-switch], brave, firecrawl [`/v2/search` — `data.web[]`, optional inline scrape via `scrapeOptions`]; behind the `SearchProvider` port) · `agent/` (search-browser-agent [thin C-1 `BrowserAgent`: search → polite-fetch → page material; optional Tier-4 `inlineScrape` reuses the provider's search-time scrape but routes each URL through `PoliteFetcher.checkAccess` (rate-limit always + robots when `RESPECT_ROBOTS_TXT=true`) first — same access policy whether fetched or reused]) · `feed/` (rss-feed-reader) · `llm/` (anthropic, openai, stub, pricing, json-recovery) · `evidence-store/` (local-fs + s3/r2; atomic temp-then-rename write [local] / bodies-then-meta + write-once [s3] + structural-completeness verify on read; `save()` rejects hollow captures; `getArtifact(id,kind)` reads ONE artifact's bytes back (off disk / S3 GetObject) for the gated evidence-fetch endpoint; both name bundle files + content-types from the domain `evidence-layout`) · `db/` (in-memory, postgres+drizzle [tuned pool + statement_timeout + `pool.on('error')`], `postgres/db-resilience` [transient-error classification + bounded retry], migrate) · `http/` (review-api [gated admin `/api/*`; `PATCH /api/candidates/:id` edit, `GET /api/candidates` filters/pagination, `POST /api/field-proposals/:key/promote`, `POST /api/manual-capture-tasks/:id/complete`, and the Bearer-GATED `GET /api/evidence/:id/:artifact` (streams evidence bytes via the injected `EvidenceStore.getArtifact` + `sendBytes`, `private, no-store`); Bearer-gated writes, zod bodies, typed-error→status via `mapErrors`], admin-evidence-dto [the gated `/api/candidates` evidence projection — keeps the raw store `*_ref`s (NOT a public allow-list) and ADDS `evidence_screenshot_url`/`evidence_html_url`/`evidence_terms_url` as RELATIVE authed paths to `/api/evidence/:id/:kind` (NOT the public CDN), ACR-13], public-api [unauthenticated read-only `/v1/*` published feed; CORS; total dispatch], public-dto [the curated public projection + freshness trust badge + `cdnBaseUrl` screenshot URL via the shared `resolveEvidenceUrl`; no-leak contract-tested], test-page) · `cli/` (`serve` mounts public-api + review-api on one port) · `suffix/` (Step 6: `tldts-suffix-oracle` — the real Public Suffix List behind the `SuffixOracle` port, wrapping the pinned `tldts` package; the ONE place a PSL vendor is imported) · `seeds/` · `logger/` · `alerting/` (Step 5: `noop-alerter` [default off-switch — logs at debug, delivers nowhere], `webhook-alerter` [POSTs JSON; Slack-renders `text` + structured event for a generic collector; timeout-bounded, failures swallowed]; behind the `Alerting` port, shared contract suite; config `ALERT_KIND`/`ALERT_WEBHOOK_URL`) · `shared/` (retry, `screenshot-download` [bounded screenshot-ref→bytes resolver shared by the firecrawl fetcher + the agent inline-scrape; null-not-empty for the evidence invariant])
- `src/composition/container.ts` — the single composition root · `src/config/` — env→typed config (zod) · `docker-entrypoint.sh` — applies migrations on container start, then runs the CLI
- `deploy/` — **unattended-scheduling templates (Step 4, external-cron model):** `deploy/k8s/cronjobs.yaml` (ConfigMap + Secret + 4 CronJobs — `crawl --due` 6h / `monitor --due` 3h / `ingest --community-due` hourly / `discover --broad` daily, **discover `suspend: true`**), `.github/workflows/scheduled.yml` (a guarded opt-in scheduled Action alternative), `deploy/README.md` (cadence rationale + env/secrets + trust posture). Safe-by-default: Tier-4 off, agentic lanes dark, `EVIDENCE_STORE=s3` required under cron (ephemeral pod FS), `concurrencyPolicy: Forbid`.
- `test/` — `contracts/` (port suites), `fixtures/golden/`, `golden/`, `fakes/`, `factories/`, `integration/` (real Container+Postgres, hermetic), `live/` (real sites+LLM, scheduled) · `drizzle/` — generated migrations · CI: `.github/workflows/{ci,live}.yml`
- `docs/` — design + seed list · `docs/api/` — **the API contract:** `openapi.yaml` (OpenAPI 3.1 source-of-truth for `/api/*` + `/v1/*` + test page) + the generated `dealroute.postman_collection.json` (regen with `npm run api:postman`; see `.claude/rules/api-and-openapi.md`) · `docs/LOCAL_DEV.md` — local cross-repo setup (Postgres + serve) · `docs/KNOWN_ISSUES.md` — deferred-findings register (see Working habits) · `ARCHITECTURE.md` — layers + how to add a source/model/condition

## Working habits
- Small, reviewable commits; tests for all pure logic before wiring I/O.
- **Every new/changed feature gets unit AND integration tests** (and a live smoke test for new external edges). The three tiers + exactly what each feature must add are in `.claude/rules/testing.md` — follow it.
- **Touch the HTTP API → update the OpenAPI spec in the same change.** `docs/api/openapi.yaml` is the source-of-truth contract for `/api/*`, `/v1/*`, and the test page; the Postman collection (`docs/api/dealroute.postman_collection.json`) is generated from it. Any route / request / response / auth / status change ⇒ edit the spec, run `npm run api:lint` + `npm run api:postman`, and commit both. Full rule + the trust constraint (the public DTO stays an allow-list — no internal field leaks into `PublicDeal`) in `.claude/rules/api-and-openapi.md`. The `code-reviewer` agent flags an out-of-sync spec as a Blocker.
- **Live testing (real sites + real LLM).** When asked to run a "real/live test", record results using the canonical template `docs/testing/LIVE_TEST_TEMPLATE.md` (copy to `docs/testing/results/LIVE_TEST_<date>.md`, fill every section incl. per-deal score-reasoning, evidence, and the trust-invariant checklist). **Extend that template whenever a new feature adds a recordable field** (new per-deal field, tier, trust guard, cost signal) — it's the contract for a complete record; add a changelog entry there. When the user wants results shown **visually**, render a filled copy as an HTML artifact (metric-card summary + per-tier deal cards + score-reasoning + trust checklist) via the visual artifact tool, keeping the Markdown copy as the source-of-truth record. Live runs need real keys in the gitignored `.env` (copy from the main repo root, delete after) — see `docs/Firecrawl_Integration_Reference.md` for the Tier-4 search backend.
- After scaffolding, **update Commands + Repo layout above**.
- **Ask before** changing the deal-record schema, the verification rules, or anything affecting trust.
- Build **Phase A** (deterministic core) first and keep it working; B/C slot in.
- **Log deferred findings.** When you spot a real issue (bug, fragility, tech debt, gap, open design question) that you are NOT fixing in the current change, append an entry to `docs/KNOWN_ISSUES.md` using the template there (severity, location, what, why-deferred, fix-when) — so nothing real is silently forgotten. Fix-it-now still wins for anything trivial or trust-critical; this is for the "by the way, this is worth fixing later" findings. Check `docs/KNOWN_ISSUES.md` when starting related work.
- Helpers: skills in `.claude/skills/` (`add-source`, `dry-run-extract`, `promote-field-proposal`); review agents in `.claude/agents/` (`code-reviewer`, `extraction-evaluator`).
